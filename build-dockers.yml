config:
  # Backtrack is where the root of the project is, relative to this file
  backtrack: .
  # Use this for prefix when building
  prefix: opensearch-php-local
images:
  opensearch-php:
    type: prod
    context: .
    dockerfile: docker/Dockerfile
